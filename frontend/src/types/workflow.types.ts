export interface WorkflowNodeData {
    title: string;
    description?: string;
}

export interface WorkflowNodeData {
    title: string
    description?: string
    stepType?: string
    delay?: number
    agent?: string
}